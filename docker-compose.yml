services:
  zerosnippet:
    build:
      context: .
      dockerfile: Dockerfile
    image: zerosnippet:latest
    container_name: zerosnippet
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
